# Copy this file to .streamlit/secrets.toml for local PostgreSQL dev,
# OR add these as secrets in Streamlit Community Cloud dashboard.
# DO NOT commit secrets.toml to git.

# ── Supabase PostgreSQL connection ─────────────────────────────────────────────
# Get this from: Supabase dashboard → Settings → Database → Connection string
# Use the "URI" format and replace [YOUR-PASSWORD] with your DB password.
DATABASE_URL = "postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres"

# ── OpenAI ─────────────────────────────────────────────────────────────────────
OPENAI_API_KEY = "sk-..."

# ── App access password ────────────────────────────────────────────────────────
APP_PASSWORD = "euroschool7"
